#include <stdio.h>
#include "main.h"

/**
 * main - print the name of the program
 * @argcwz: number of argument
 * @argv: array of argument
 *
 * Return: Always 0 (validation)
 */
int main(int argcwz __attribute__((unused)), char *argv[])
{
printf("%s\n", *argv);

return (0);
}

