#include "main.h"
/**
 * _abs - computes all the absolute values of an integer
 * @i: input number as an integer.
 *
 * Return: absolute value always.
 */
int _abs(int i)
{
if (i >= 0)
{
return (i);
}
else
{
return (i * -1);
}
}
